runtime: nodejs16

handlers:
  - url: /resources
    static_dir: resources

  - url: /.*
    secure: always
    redirect_http_response_code: 301
    script: auto

env_variables:
  CREDS: <%= CREDS %>
